% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_corr_spec.R
\name{new_corr_spec}
\alias{new_corr_spec}
\title{Build a correlation display specification}
\usage{
new_corr_spec(var1, var2, ...)
}
\arguments{
\item{var1}{Character vector of first variable names per pair.}

\item{var2}{Character vector of second variable names per pair.}

\item{...}{Named character vectors of equal length to \code{var1}/\code{var2}.
Each becomes one display row inside the cell (e.g. \code{rho}, \code{pval}, \code{bf}).}
}
\value{
An object of class \code{corr_spec}.
}
\description{
Constructs a structured spec object consumed by \code{corr_matrix()}. Always
requires \code{var1} and \code{var2} — the pair pattern they encode determines which
triangle(s) of the matrix are filled:
}
\details{
\tabular{ll}{
   Pattern \tab Fills \cr
   \code{var1 < var2} \tab Lower triangle only \cr
   \code{var1 <= var2} \tab Lower triangle + diagonal \cr
   \code{var1 > var2} \tab Upper triangle only \cr
   \code{var1 >= var2} \tab Upper triangle + diagonal \cr
   \code{var1 != var2} \tab Both triangles, no diagonal \cr
   \code{var1 == var2} \tab Full matrix (diag forced to 1) \cr
}


All additional named vectors become display rows inside each cell, rendered
in the order they are supplied.
}
\examples{
new_corr_spec(
    var1 = c("a", "a", "b"),
    var2 = c("b", "c", "c"),
    rho  = c("0.89", "0.79", "0.66"),
    pval = c("<0.001", "<0.001", "<0.001")
)

}
