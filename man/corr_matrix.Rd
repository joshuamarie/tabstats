% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-corr_matrix.R
\name{corr_matrix}
\alias{corr_matrix}
\title{Display a Correlation Matrix Table in the Console}
\usage{
corr_matrix(
  display,
  method = NULL,
  diag_1 = TRUE,
  digits = 3,
  center_table = FALSE,
  border_char = getOption("tab_default")$border_char,
  style = list(),
  ...
)
}
\arguments{
\item{display}{A \code{corr_spec} object from \code{new_corr_spec()}, or a plain
symmetric matrix (e.g. from \code{cor()}).}

\item{method}{Label shown in the title (e.g. \code{"Pearson"}). Auto-detected
from a \code{method} attribute on the spec if present.}

\item{diag_1}{If \code{TRUE}, diagonal cells always show \code{"1"}. Default \code{TRUE}.}

\item{digits}{Decimal places for numeric formatting. Default \code{3}.}

\item{center_table}{Center table in terminal? Default \code{FALSE}.}

\item{border_char}{Border character. Default from \code{getOption("tab_default")}.}

\item{style}{Named list of style specs. Keys: \code{corr} and any extra field
names passed to \code{new_corr_spec()} (e.g. \code{pval}, \code{bf}), plus \code{title} and
\code{border_text}. Each value can be a cli-style string or a \verb{function(x)}.}

\item{...}{Reserved for future use.}
}
\value{
Invisibly returns the rendered character matrix.
}
\description{
Display a Correlation Matrix Table in the Console
}
\examples{
# From a plain matrix
corr_matrix(cor(mtcars[, 1:4]), method = "Pearson")

# From a corr_spec
spec = new_corr_spec(
    var1 = c("a", "a", "b"),
    var2 = c("b", "c", "c"),
    rho  = c("0.89", "0.79", "0.66"),
    pval = c("<0.001", "<0.001", "<0.001")
)
corr_matrix(spec, method = "Pearson")

}
